/**
 * Detect Cordova / PhoneGap / Ionic frameworks on a mobile device.
 *
 * Deliberately does not rely on checking `file://` URLs (as this fails PhoneGap in the Ripple emulator) nor
 * Cordova `onDeviceReady`, which would normally wait for a callback.
 *
 * @return {boolean} isMobileCordova
 */
export declare const isMobileCordova: () => boolean;
/**
 * Detect if device is running in native environment as opposite to browser platform
 *
 * @return {boolean} Is running in native
 */
export declare const isNative: () => boolean;
export declare const isCordovaAndroid: () => boolean;
export declare const isCordovaIOS: () => boolean;
