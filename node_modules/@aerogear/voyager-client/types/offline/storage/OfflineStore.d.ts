import { OperationQueueEntry, OfflineItem } from "../OperationQueueEntry";
import { SyncConfig } from "../../config/SyncConfig";
/**
 * Abstract Offline storage
 */
export declare class OfflineStore {
    private storage;
    private offlineMetaKey;
    private arrayOfKeys;
    constructor(config: SyncConfig);
    /**
     * Init store
     */
    init(): Promise<void>;
    /**
     * Save an entry to store
     *
     * @param entry - the entry to be saved
     */
    saveEntry(entry: OperationQueueEntry): Promise<void>;
    /**
     * Remove an entry from the store
     *
     * @param queue - the entry to be removed
     */
    removeEntry(entry: OperationQueueEntry): Promise<void>;
    /**
     * Fetch data from the offline store
     */
    getOfflineData(): Promise<OfflineItem[]>;
    private getOfflineKey;
}
