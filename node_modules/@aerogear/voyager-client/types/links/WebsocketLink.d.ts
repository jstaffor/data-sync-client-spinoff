import { WebSocketLink } from "apollo-link-ws";
import { DataSyncConfig } from "../config";
export declare const defaultWebSocketLink: (userOptions: DataSyncConfig, config: WebSocketLink.Configuration) => WebSocketLink;
