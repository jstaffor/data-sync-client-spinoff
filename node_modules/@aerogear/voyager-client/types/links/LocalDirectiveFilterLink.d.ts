import { ApolloLink, NextLink, Operation } from "apollo-link";
import * as debug from "debug";
export declare const logger: debug.Debugger;
export declare class LocalDirectiveFilterLink extends ApolloLink {
    private readonly directiveRemovalConfig;
    constructor();
    request(operation: Operation, forward: NextLink): import("apollo-link").Observable<import("apollo-link").FetchResult<{
        [key: string]: any;
    }, Record<string, any>, Record<string, any>>>;
}
