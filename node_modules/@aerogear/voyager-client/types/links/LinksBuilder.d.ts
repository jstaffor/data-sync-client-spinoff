import { ApolloLink } from "apollo-link";
import { DataSyncConfig } from "../config";
import { OfflineLink } from "../offline/OfflineLink";
import { OfflineStore } from "../offline";
/**
 * Method for creating "uber" composite Apollo Link implementation including:
 *
 * - Http support
 * - Websocket support
 * - Offline handling
 * - Conflict resolution
 * - Audit logging
 * - File uploads
 */
export declare const createDefaultLink: (config: DataSyncConfig, offlineLink: ApolloLink) => Promise<ApolloLink>;
/**
 * Create offline link
 */
export declare const createOfflineLink: (config: DataSyncConfig, store: OfflineStore) => Promise<OfflineLink>;
/**
 * Default HTTP Apollo Links
 * Provides out of the box functionality for:
 *
 * - Offline/Online queue
 * - Conflict resolution
 * - Error handling
 * - Audit logging
 */
export declare const defaultHttpLinks: (config: DataSyncConfig, offlineLink: ApolloLink) => Promise<ApolloLink>;
