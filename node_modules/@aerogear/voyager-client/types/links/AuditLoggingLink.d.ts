import { ApolloLink, NextLink, Operation } from "apollo-link";
export declare class AuditLoggingLink extends ApolloLink {
    private readonly clientId;
    private readonly metricsPayload;
    constructor(clientId: string, metricsPayload: any);
    request(operation: Operation, forward?: NextLink): import("apollo-link").Observable<import("apollo-link").FetchResult<{
        [key: string]: any;
    }, Record<string, any>, Record<string, any>>> | null;
}
